---
import InstallCommand from './InstallCommand.astro';
---

<section class="hero">
  <div class="container">
    <div class="hero-content animate-fade-in">
      <h1 class="hero-title">
        Claude Code Skills Hub<br/>
        技能聚合与场景化打包平台
      </h1>
      <p class="hero-description">
        汇集 GitHub 高星开源项目的 240+ 精选技能，精心打造 21 个场景化技能包，
        覆盖开发、科研、办公等多个领域。让每一次 Claude Code 对话都拥有专家级能力。
      </p>
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">240+</span>
          <span class="stat-label">技能</span>
        </div>
        <div class="stat">
          <span class="stat-number">16</span>
          <span class="stat-label">大领域</span>
        </div>
        <div class="stat">
          <span class="stat-number">21</span>
          <span class="stat-label">场景包</span>
        </div>
        <div class="stat">
          <span class="stat-number">9+</span>
          <span class="stat-label">开源项目</span>
        </div>
      </div>

      <!-- 快速安装入口 -->
      <div class="quick-install-section">
        <div class="quick-install-header">
          <h3 class="quick-install-title">快速开始</h3>
          <p class="quick-install-desc">一键安装全栈开发者套件，包含 6 个核心技能</p>
        </div>
        <div class="quick-install-command">
          <InstallCommand packId="fullstack-developer" />
        </div>
        <div class="quick-install-actions">
          <a href="#all-packages" class="btn btn-secondary">浏览更多技能包</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--spacing-2xl) var(--spacing-md);
    text-align: center;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto var(--spacing-lg);
    line-height: 1.7;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    gap: var(--spacing-xl);
    flex-wrap: wrap;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-primary);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    margin-top: var(--spacing-xs);
  }

  @media (max-width: 768px) {
    .hero {
      padding: var(--spacing-xl) var(--spacing-sm);
    }

    .hero-stats {
      gap: var(--spacing-md);
    }

    .stat-number {
      font-size: 2rem;
    }

    .quick-install-section {
      margin-top: var(--spacing-lg);
      padding: var(--spacing-md);
    }

    .quick-install-title {
      font-size: 1.25rem;
    }
  }

  /* 快速安装区域样式 */
  .quick-install-section {
    margin-top: var(--spacing-xl);
    padding: var(--spacing-lg);
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.08) 0%, rgba(118, 75, 162, 0.08) 100%);
    border: 1px solid rgba(102, 126, 234, 0.2);
    border-radius: var(--radius-lg);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .quick-install-header {
    margin-bottom: var(--spacing-md);
  }

  .quick-install-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0 0 var(--spacing-xs);
    color: var(--color-text);
  }

  .quick-install-desc {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    margin: 0;
  }

  .quick-install-command {
    margin-bottom: var(--spacing-md);
  }

  .quick-install-actions {
    display: flex;
    justify-content: center;
  }

  .quick-install-actions .btn {
    padding: var(--spacing-sm) var(--spacing-lg);
    font-size: 0.9375rem;
  }
</style>
