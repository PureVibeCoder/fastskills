---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import WhyChoose from '../components/WhyChoose.astro';
import SkillSources from '../components/SkillSources.astro';
import AllPackages from '../components/AllPackages.astro';
import CategoryFilter from '../components/CategoryFilter.astro';
import SkillGrid from '../components/SkillGrid.astro';
import { skills } from '../data/skills';
import { categories } from '../data/categories';
import { downloadSingleSkill } from '../utils/downloader';
import { downloadSkillPack, filterSkillsForPack, skillPacks } from '../utils/packager';
---

<Layout title="FastSkills - 智能技能编排控制器">
  <Hero />

  <section id="features" class="features-section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">
          <span class="bracket">[</span> SYSTEM_CORE_FEATURES <span class="bracket">]</span>
        </h2>
      </div>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <div class="feature-icon">🗂️</div>
          </div>
          <h3 class="feature-title">SKILL_AGGREGATION</h3>
          <p class="feature-description">汇集 9+ 个 GitHub 高星开源项目的 240+ 精选技能，按 16 大类别科学分类</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <div class="feature-icon">📦</div>
          </div>
          <h3 class="feature-title">SCENARIO_PACKS</h3>
          <p class="feature-description">精心打造 22 个场景化技能包，覆盖开发、科研、办公等多个领域</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <div class="feature-icon">🔍</div>
          </div>
          <h3 class="feature-title">SMART_SEARCH</h3>
          <p class="feature-description">支持按名称、描述、分类快速定位所需技能</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <div class="feature-icon">⬇️</div>
          </div>
          <h3 class="feature-title">SECURE_INTEGRATION</h3>
          <p class="feature-description">提供技能下载并倡导安全审查，支持手动集成到本地环境</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon-wrapper">
            <div class="feature-icon">📚</div>
          </div>
          <h3 class="feature-title">FULL_DOCUMENTATION</h3>
          <p class="feature-description">每个技能都有完整的文档和使用示例</p>
        </div>
      </div>
    </div>
  </section>

  <WhyChoose />

  <SkillSources />

  <AllPackages />

  <div class="skills-container">
    <div class="container">
      <div class="skills-header">
        <h2 class="section-title terminal-style">
          <span class="prompt">&gt;</span> ACCESS_SKILL_LIBRARY<span class="cursor">_</span>
        </h2>
        <button class="btn download-pack-btn" id="download-pack">
          <span class="btn-icon">📦</span> INITIALIZE_DOWNLOAD
        </button>
      </div>
      <CategoryFilter categories={categories} selectedCategory={null} />
    </div>
    <SkillGrid skills={skills} selectedCategory={null} />
  </div>
</Layout>

<style>
  .features-section {
    padding: var(--spacing-4xl) 0;
    background: var(--color-bg);
    position: relative;
    overflow: hidden;
  }

  .features-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(0, 240, 255, 0.3), transparent);
  }

  .section-header {
    text-align: center;
    margin-bottom: var(--spacing-3xl);
  }

  .section-title {
    font-family: var(--font-display);
    font-size: 2rem;
    color: var(--color-text);
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .bracket {
    color: var(--color-primary);
    font-family: var(--font-mono);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
    padding: 0 var(--spacing-md);
    max-width: 1400px;
    margin: 0 auto;
  }

  .feature-card {
    background: rgba(13, 17, 23, 0.4);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(230, 237, 243, 0.1);
    padding: var(--spacing-xl);
    position: relative;
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    opacity: 0;
    animation: slideIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .feature-card:nth-child(1) { animation-delay: 0.1s; }
  .feature-card:nth-child(2) { animation-delay: 0.2s; }
  .feature-card:nth-child(3) { animation-delay: 0.3s; }
  .feature-card:nth-child(4) { animation-delay: 0.4s; }
  .feature-card:nth-child(5) { animation-delay: 0.5s; }

  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .feature-card:hover {
    border-color: var(--color-primary);
    box-shadow: 0 0 30px rgba(0, 240, 255, 0.1);
    transform: translateY(-5px);
  }

  .feature-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle at top right, rgba(0, 240, 255, 0.05), transparent 40%);
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
  }

  .feature-card:hover::after {
    opacity: 1;
  }

  .feature-icon-wrapper {
    width: 60px;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 240, 255, 0.05);
    border: 1px solid rgba(0, 240, 255, 0.2);
    margin-bottom: var(--spacing-lg);
    box-shadow: 0 0 15px rgba(0, 240, 255, 0.1);
  }

  .feature-icon {
    font-size: 2rem;
  }

  .feature-title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    color: var(--color-text);
    margin-bottom: var(--spacing-md);
    font-weight: 600;
    letter-spacing: 0.05em;
  }

  .feature-description {
    color: var(--color-text-secondary);
    font-size: 0.9375rem;
    line-height: 1.6;
    margin: 0;
    font-family: var(--font-mono);
  }

  .skills-container {
    padding: var(--spacing-3xl) 0;
    background: var(--color-bg-secondary);
    border-top: 1px solid rgba(230, 237, 243, 0.1);
  }

  .skills-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--spacing-xl);
    padding: 0 var(--spacing-md);
    max-width: 1400px;
    margin-left: auto;
    margin-right: auto;
  }

  .terminal-style {
    font-family: var(--font-mono);
    font-size: 1.5rem;
    color: var(--color-text);
    margin: 0;
    display: flex;
    align-items: center;
  }

  .prompt {
    color: var(--color-secondary);
    margin-right: 0.5rem;
  }

  .cursor {
    display: inline-block;
    width: 10px;
    height: 2px;
    background: var(--color-primary);
    margin-left: 5px;
    animation: blink 1s step-end infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .download-pack-btn {
    background: transparent;
    border: 1px solid var(--color-primary);
    color: var(--color-primary);
    font-family: var(--font-mono);
    padding: 0.75rem 1.5rem;
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-transform: uppercase;
  }

  .download-pack-btn:hover {
    background: rgba(0, 240, 255, 0.1);
    box-shadow: 0 0 20px rgba(0, 240, 255, 0.3);
    text-shadow: 0 0 5px var(--color-primary);
  }

  /* 技能详情模态框 */
  .skill-detail-name {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-primary);
    margin-bottom: var(--spacing-sm);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .skill-detail-meta {
    display: flex;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-md);
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    font-family: var(--font-mono);
  }

  .skill-detail-triggers {
    margin-bottom: var(--spacing-md);
    background: rgba(0, 240, 255, 0.05);
    border: 1px solid rgba(0, 240, 255, 0.2);
    padding: var(--spacing-md);
  }

  .skill-detail-triggers h4 {
    margin-bottom: var(--spacing-xs);
    color: var(--color-secondary);
    font-family: var(--font-mono);
    text-transform: uppercase;
    font-size: 0.875rem;
  }

  @media (max-width: 768px) {
    .skills-header {
      flex-direction: column;
      gap: var(--spacing-md);
      text-align: center;
    }
    
    .section-title {
      font-size: 1.5rem;
    }
  }
</style>

<style is:global>
  /* 模态框样式 - 全局样式用于动态创建的模态框 */
  .modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(3, 4, 7, 0.85);
    backdrop-filter: blur(8px);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
  }

  .modal-content {
    background: rgba(13, 17, 23, 0.95);
    border: 1px solid var(--color-primary);
    border-radius: 0;
    max-width: 600px;
    width: 90%;
    max-height: 80vh;
    overflow-y: auto;
    box-shadow: 0 0 50px rgba(0, 240, 255, 0.15);
    position: relative;
  }

  .modal-content::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--color-primary);
    box-shadow: 0 0 10px var(--color-primary);
  }

  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-lg);
    border-bottom: 1px solid rgba(230, 237, 243, 0.1);
  }

  .modal-header h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: 1.5rem;
    color: var(--color-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .modal-close {
    background: transparent;
    border: 1px solid rgba(230, 237, 243, 0.2);
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--color-text-secondary);
    transition: all 0.2s;
  }

  .modal-close:hover {
    color: var(--color-secondary);
    border-color: var(--color-secondary);
    box-shadow: 0 0 10px rgba(255, 46, 99, 0.3);
  }

  .modal-body {
    padding: var(--spacing-lg);
    color: var(--color-text);
  }

  .modal-description {
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-lg);
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }

  .pack-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .pack-button {
    padding: var(--spacing-md);
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(230, 237, 243, 0.1);
    border-radius: 0;
    text-align: left;
    cursor: pointer;
    transition: all 0.2s;
    position: relative;
    overflow: hidden;
  }

  .pack-button:hover {
    border-color: var(--color-primary);
    background: rgba(0, 240, 255, 0.05);
    box-shadow: 0 0 15px rgba(0, 240, 255, 0.1);
  }

  .pack-button::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--color-primary);
    transform: scaleY(0);
    transition: transform 0.2s ease;
  }

  .pack-button:hover::before {
    transform: scaleY(1);
  }

  .pack-name {
    font-family: var(--font-display);
    font-weight: 600;
    margin-bottom: var(--spacing-xs);
    color: var(--color-text);
    text-transform: uppercase;
  }

  .pack-button:hover .pack-name {
    color: var(--color-primary);
  }

  .pack-description {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    font-family: var(--font-mono);
  }

  /* Scrollbar for modal */
  .modal-content::-webkit-scrollbar {
    width: 6px;
  }
  .modal-content::-webkit-scrollbar-track {
    background: rgba(0, 0, 0, 0.3);
  }
  .modal-content::-webkit-scrollbar-thumb {
    background: rgba(230, 237, 243, 0.2);
  }
  .modal-content::-webkit-scrollbar-thumb:hover {
    background: var(--color-primary);
  }
</style>

<script type="module" define:vars={{ skills }}>
  // 技能数据（从服务器传递）
  const skillsData = skills;

  // 监听分类按钮点击
  document.addEventListener('click', (e) => {
    const target = e.target;
    const categoryButton = target.closest('.category-button');

    if (categoryButton) {
      const category = categoryButton.dataset.category;

      // 更新按钮状态
      document.querySelectorAll('.category-button').forEach(btn => {
        btn.classList.remove('active');
      });
      categoryButton.classList.add('active');

      // 过滤技能卡片
      document.querySelectorAll('.skill-card').forEach(card => {
        if (category === 'all' || card.dataset.category === category) {
          card.style.display = '';
        } else {
          card.style.display = 'none';
        }
      });
    }
  });

  // 创建模态框工具函数
  function createModal(config) {
    const modalHtml = `
      <div class="modal-overlay" id="${config.id}">
        <div class="modal-content">
          <div class="modal-header">
            <h2>${config.title}</h2>
            <button class="modal-close">&times;</button>
          </div>
          <div class="modal-body">${config.content}</div>
        </div>
      </div>
    `;

    document.body.insertAdjacentHTML('beforeend', modalHtml);

    const modal = document.getElementById(config.id);
    const closeBtn = modal?.querySelector('.modal-close');

    const close = () => {
      modal?.remove();
      config.onClose?.();
    };

    closeBtn?.addEventListener('click', close);
    modal?.addEventListener('click', (e) => {
      if (e.target === modal) close();
    });

    return { modal, close };
  }

  // 下载单个技能文件
  function downloadSingleSkill(skill) {
    const blob = new Blob([skill.content], { type: 'text/markdown' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `${skill.id}-SKILL.md`;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
  }

  // 单个下载功能
  document.addEventListener('click', (e) => {
    const target = e.target;
    const downloadBtn = target.closest('.btn-download');

    if (downloadBtn) {
      const skillId = downloadBtn.dataset.skillId;
      const skillName = downloadBtn.dataset.skillName;
      
      // 如果 skillsData 为空，从 DOM 创建技能对象
      let skill = skillsData.find(s => s.id === skillId);
      if (!skill) {
        skill = {
          id: skillId,
          name: skillName,
          content: `# ${skillName}\n\nThis is a skill downloaded from FastSkills.\n\n## Description\n\nPlease refer to the original source for more details.\n\n---\n\nFastSkills: https://github.com/PureVibeCoder/fastskills`
        };
      }

      downloadSingleSkill(skill);

      const originalText = downloadBtn.textContent;
      downloadBtn.textContent = '已下载!';
      downloadBtn.classList.remove('btn-primary');
      downloadBtn.classList.add('btn-success');

      setTimeout(() => {
        downloadBtn.textContent = originalText;
        downloadBtn.classList.remove('btn-success');
        downloadBtn.classList.add('btn-primary');
      }, 2000);
    }
  });

  // 查看详情功能
  document.addEventListener('click', (e) => {
    const target = e.target;
    const viewBtn = target.closest('.btn-view');

    if (viewBtn) {
      const skillName = viewBtn.dataset.skillName;
      const skillDescription = viewBtn.dataset.skillDescription;
      const skillTriggers = viewBtn.dataset.skillTriggers;

      createModal({
        id: 'skill-detail-modal',
        title: '技能详情',
        content: `
          <div class="skill-detail-name">${skillName}</div>
          <p class="skill-detail-description">${skillDescription}</p>
          <div class="skill-detail-triggers">
            <h4>触发词：</h4>
            <p>${skillTriggers}</p>
          </div>
        `
      });
    }
  });

  // 技能包配置
  const skillPacks = {
    'frontend-developer': { name: '前端开发者套件', description: '适合前端工程师，包含 UI 设计、组件开发、响应式布局等技能' },
    'fullstack-developer': { name: '全栈开发者套件', description: '覆盖前后端开发全流程，适合全栈工程师' },
    'document-production': { name: '文档生产套件', description: '专注于文档生成和处理，适合技术写作者' },
    'knowledge-management': { name: '知识管理套件', description: 'Obsidian 笔记系统、知识库管理、可视化' },
    'devops-engineer': { name: 'DevOps 工程师套件', description: 'CI/CD、容器化、部署自动化相关技能' },
    'mcp-developer': { name: 'MCP 开发者套件', description: 'Model Context Protocol 服务器和工具开发' },
    'task-planning': { name: '任务规划套件', description: 'Manus 风格文件规划、任务追踪、上下文工程' },
    'testing-qa': { name: '测试与质量保障套件', description: '自动化测试、代码审查、质量保障' },
    'backend-specialist': { name: '后端专家套件', description: 'API 设计、数据库、认证授权相关技能' },
    'design-system': { name: '设计系统套件', description: 'UI 设计、主题、品牌相关技能' },
    'content-creator': { name: '内容创作套件', description: '适合内容创作者和营销人员' },
    'research-analyst': { name: '研究分析套件', description: '深度研究和数据分析相关技能' },
    'media-processing': { name: '媒体处理套件', description: '图片、视频等媒体文件处理' },
    'productivity-tools': { name: '效率工具套件', description: '提升日常工作效率的实用技能' },
    'drug-discovery': { name: '药物发现套件', description: '虚拟筛选、分子对接、ADMET 预测、化合物优化' },
    'genomics-bioinformatics': { name: '基因组学与生物信息学套件', description: '序列分析、单细胞 RNA-seq、基因调控网络' },
    'clinical-research': { name: '临床研究套件', description: '临床试验、变异解读、药物基因组学、精准医疗' },
    'ml-deep-learning': { name: '机器学习与深度学习套件', description: 'PyTorch、scikit-learn、强化学习、模型解释' },
    'quantum-physics': { name: '量子计算与物理套件', description: '量子计算、天文学、材料科学' },
    'data-visualization': { name: '科学数据可视化套件', description: '统计分析、网络可视化、出版级图表' },
    'scientific-databases': { name: '科学数据库套件', description: 'PubMed、UniProt、KEGG 等科学数据库访问' },
    'scientific-writing': { name: '科学写作与交流套件', description: '论文写作、同行评审、海报制作、文献管理' },
    'proteomics-multiomics': { name: '蛋白质组学与多组学套件', description: '质谱分析、蛋白质工程、多组学整合' },
    'lab-automation': { name: '实验室自动化套件', description: '液体处理、实验流程自动化、LIMS 集成' },
    'medical-imaging': { name: '医学影像与病理套件', description: 'DICOM 处理、全切片分析、计算病理学' }
  };

  // 过滤技能
  function filterSkillsForPack(allSkills, packKey) {
    const pack = skillPacks[packKey];
    if (!pack) return allSkills;

    // 根据套件名称中的关键词过滤
    const keywords = pack.name.split(/[和与]/)[0];
    const categoryMap = {
      '前端': 'frontend',
      '后端': 'backend',
      '测试': 'testing',
      'DevOps': 'devops',
      '文档': 'document',
      '知识管理': 'knowledge',
      '媒体': 'media',
      '内容创作': 'content',
      '研究分析': 'research',
      '效率': 'productivity',
      '设计': 'design',
      '药物': 'cheminformatics',
      '基因组': 'bioinformatics',
      '临床': 'clinical',
      '机器学习': 'ml-ai',
      '量子': 'physics-materials',
      '数据可视化': 'data-viz',
      '科学数据库': 'sci-databases',
      '科学写作': 'sci-communication',
      '蛋白质组': 'scientific',
      '实验室': 'lab-automation',
      '医学影像': 'clinical'
    };

    let categoryId = null;
    for (const [key, value] of Object.entries(categoryMap)) {
      if (keywords.includes(key)) {
        categoryId = value;
        break;
      }
    }

    if (!categoryId) return allSkills.slice(0, 5);
    return allSkills.filter(s => s.category.id === categoryId);
  }

  // 下载技能包
  async function downloadSkillPack(skills, packName) {
    const JSZip = (await import('jszip')).default;

    const zip = new JSZip();
    
    // 从统一的映射文件导入技能源信息
    async function fetchSkillSourceMapping() {
      const response = await fetch('/api/skill-sources');
      if (response.ok) {
        return await response.json();
      }
      return {};
    }

    // 获取技能内容（优先从 API 读取，否则使用现有内容）
    async function getSkillContent(skill, skillToSource) {
      const sourceInfo = skillToSource[skill.id];
      if (sourceInfo) {
        try {
          const response = await fetch(`/api/skill-content?source=${sourceInfo.source}&path=${sourceInfo.path}`);
          if (response.ok) {
            return await response.text();
          }
        } catch (error) {
          console.warn(`[index] Failed to fetch SKILL.md for ${skill.id}:`, error);
        }
      }
      return skill.content;
    }

    // 并行获取所有技能内容
    const skillToSource = await fetchSkillSourceMapping();
    const skillContents = await Promise.all(
      skills.map(async (skill) => {
        const content = await getSkillContent(skill, skillToSource);
        return { id: skill.id, name: skill.name, content };
      })
    );

    skillContents.forEach(skill => {
      zip.file(`${skill.id}/SKILL.md`, skill.content);
    });

    const readme = `# ${packName}

本技能包由 FastSkills (https://github.com/PureVibeCoder/fastskills) 聚合提供，共包含 ${skills.length} 个技能。

> [!CAUTION]
> **安全警告**：本技能包收集自第三方开源项目。在使用之前，请务必对技能目录下的 \`SKILL.md\` 文件进行安全审查，确保其指令、触发器和操作指南符合您的预期，且不会执行恶意代码。

## 包含的技能
${skills.map(s => `- **${s.name}** (\`${s.id}\`)`).join('\n')}

## 集成方法

1. 解压本文件
2. 将需要的技能目录复制到您的全局技能目录 (\`~/.claude/skills/\`) 或项目目录 (\`.claude/skills/\`)
3. 重新启动 Claude Code`;
    zip.file('README.md', readme);

    const content = await zip.generateAsync({ type: 'blob' });
    const url = URL.createObjectURL(content);
    const link = document.createElement('a');
    link.href = url;
    link.download = `${packName}-skills-pack.zip`;
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
  }

  // 技能包下载功能
  const downloadPackBtn = document.getElementById('download-pack');
  if (downloadPackBtn) {
    downloadPackBtn.addEventListener('click', async () => {
      const packList = Object.entries(skillPacks).map(([key, pack]) => `
        <button class="pack-button" data-pack="${key}">
          <div class="pack-name">${pack.name}</div>
          <div class="pack-description">${pack.description}</div>
        </button>
      `).join('');

      const { modal, close } = createModal({
        id: 'pack-modal',
        title: '下载技能包',
        content: `
          <p class="modal-description">选择要下载的技能包：</p>
          <div class="pack-list">${packList}</div>
        `
      });

      modal?.querySelectorAll('.pack-button').forEach(button => {
        button.addEventListener('click', async () => {
          const packKey = button.dataset.pack;
          const filteredSkills = filterSkillsForPack(skillsData, packKey);
          const packName = button.querySelector('.pack-name')?.textContent || 'skills';

          await downloadSkillPack(filteredSkills, packName);

          close();
        });
      });
    });
  }
</script>
